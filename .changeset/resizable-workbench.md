---
"@numenjs/components": minor
---

Add ResizeHandle for controlled split panes with pointer capture, keyboard resizing, cancellation, bounds, and reset support. Workbench uses it to resize its sidebar, inspector, and bottom panel with persistent size preferences.

Simplify Input and Textarea public component types so external Vue h() consumers compile without TypeScript union expansion errors.
